spring:
  application:
    name: music-project
  datasource:
    url: jdbc:mariadb://localhost:3306/music_recommender
    username: user
    password: Senha123
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format sql: true