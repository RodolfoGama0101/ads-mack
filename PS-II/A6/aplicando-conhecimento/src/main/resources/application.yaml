spring:
  application:
    name: aplicando-conhecimento
  datasource:
    url: jdbc:mariadb://localhost:3306/minha_carteira
    username: root
    password: root_password
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8045